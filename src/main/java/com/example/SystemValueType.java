package com.example;

/**
 * システムで使用する値を管理する列挙型クラスです。
 * 
 * @author atagawa
 *
 */
public enum SystemValueType {
    defaultLength,
    defaultLetterCase,
    defaultAcceptSymbolChar,
    defaultAcceptedSymbolChars,
    maxLength,
    outOfLetterCase,
    outputForRed,
    outputForBlack,
    newLineCode;
}
